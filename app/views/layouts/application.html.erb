<!DOCTYPE html>
<html>
  <head>
    <title>CleverFitAirbnb</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", media: 'all' , "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body>
    <nav class="navbar navbar-expand-sm navbar-light bg-white shadow-sm">
    <div class="container-fluid mx-3">
      <!-- Logo on the left -->
      <%= link_to root_path, class: 'navbar-brand navbar-dark' do %>
        <%= image_tag 'https://img.freepik.com/premium-vector/cartoon-chef-with-red-circle-background_17812-100.jpg', alt: 'Chef Airbnb Logo', width: 50, height: 50 %>
      <% end %>
      <!-- "Chef Airbnb" text in the middle -->
      <div class="navbar-text p-0">
        <h1 class="fs-3 fw-bold mb-0">Chef Airbnb</h1>
      </div>
      <!-- Upload picture or sign-in/sign-out button on the right -->
      <div class="navbar-right">
        <% if user_signed_in? %>
          <% if session[:chef_mode] %>
            <%= link_to 'Profile', chef_user_path(current_user), class: 'btn btn-dark rounded-5' %>
          <% else %>
          <%= link_to 'Profile', user_path(current_user), class: 'btn btn-dark rounded-5' %>
          <% end %>
          <%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete }, class: 'btn btn-outline-danger rounded-5' %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
  </nav>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    <%= yield %>
    <footer class="footer">
    <div class="text-center p-3">
      © 2024 Chef Airbnb
    </div>
  </footer>
  </body>
</html>
